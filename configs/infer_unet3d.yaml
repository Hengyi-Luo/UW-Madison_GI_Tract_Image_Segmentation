# Minimal inference config (override defaults as needed)
data_root: inputs
ids_csv: inputs/splits/val_case_days.csv

# Set this or pass --weights
weights: ""

# If empty, defaults to: <weights_parent>/infer
output_dir: ""

evaluate: true
