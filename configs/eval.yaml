data_root: /home/jeremiah/github/UW-Madison_GI_Tract_Image_Segmentation/inputs
weights: /home/jeremiah/github/UW-Madison_GI_Tract_Image_Segmentation/outputs/uwgi_train_unet/20260129-194749-3dunet-modified-rle/best.pt
out: ./outputs/eval/eval_summary.json
out_per_case: ./outputs/eval/eval_per_case.csv

# A file containing validation IDs:
# - .csv with a `case_day` column (recommended)
# - OR one per line: `case123_day0` OR Kaggle `case123_day0_slice_0001`
val_ids: /home/jeremiah/github/UW-Madison_GI_Tract_Image_Segmentation/inputs/splits/val_case_days.csv

# mode: sw (sliding window, default) or direct (full volume forward)
mode: sw

# sliding window params (defaults to checkpoint patch_* if omitted)
sw_batch_size: 1
overlap: 0.25
threshold: 0.5

# runtime
num_workers: 4
mixed_precision: no
per_case: false
